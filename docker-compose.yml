version: '3'
services:
  portafolio:
      build:
        context: .
        dockerfile: Dockerfile
      image: 'portafolio:latest'
      container_name: portafolio
      ports:
          - '100:80'
      restart: always
      volumes:
          - 'cv:/var/www/html/cv'
volumes:
  cv:
